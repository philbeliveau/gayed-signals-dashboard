# Explicitly include web app (whitelist approach)
!apps/web/**

# Ignore everything else in apps/ except web
apps/backend

# Ignore domains/data-pipeline (Railway deployment)
domains/data-pipeline

# Ignore Python services
python-services