{
  "name": "@gayed/data-pipeline",
  "version": "1.0.0",
  "description": "Unified Data Pipeline for Gayed Signals Dashboard",
  "private": true,
  "main": "./services/UnifiedDataService.ts",
  "scripts": {
    "dev": "ts-node server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "prisma:generate": "prisma generate --schema=./prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema=./prisma/schema.prisma",
    "prisma:deploy": "prisma migrate deploy --schema=./prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema=./prisma/schema.prisma",
    "cleanup:preview": "ts-node src/jobs/scheduled-cleanup.ts preview",
    "cleanup:execute": "ts-node src/jobs/scheduled-cleanup.ts execute",
    "benchmark:queries": "ts-node src/benchmarks/query-performance.ts",
    "benchmark:pool": "ts-node src/config/database.ts",
    "pretest": "npx prisma generate --schema=./prisma/schema.prisma",
    "test": "jest",
    "test:unit": "jest --testPathIgnorePatterns=integration",
    "test:integration": "jest --config=jest.config.integration.js",
    "test:integration:ci": "jest --config=jest.config.integration.js --ci --runInBand",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:coverage:integration": "jest --config=jest.config.integration.js --coverage",
    "test:load": "artillery run test-scenarios/load-test.yml",
    "test:stress": "artillery run test-scenarios/stress-test.yml",
    "test:spike": "artillery run test-scenarios/spike-test.yml",
    "test:all": "npm run test:unit && npm run test:integration && npm run test:load",
    "lint": "eslint ."
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "@types/compression": "^1.8.1",
    "@types/supertest": "^6.0.3",
    "axios": "^1.13.1",
    "compression": "^1.8.1",
    "express": "^4.18.2",
    "ioredis": "^5.3.2",
    "supertest": "^7.1.4",
    "uuid": "^9.0.1",
    "yahoo-finance2": "^3.10.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "artillery": "^2.0.12",
    "jest": "^29.7.0",
    "jest-html-reporter": "^3.10.2",
    "jest-junit": "^16.0.0",
    "prisma": "^5.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
